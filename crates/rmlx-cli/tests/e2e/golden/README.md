# Recorded E2E golden token-id (byte) sequences.
# One <case_id>.json per `golden`-kind manifest case: an ordered JSON array of
# per-token chosen-token byte arrays captured at temp=0 greedy via the OpenAI
# logprobs `bytes` field. Regenerate with RMLX_E2E_REGEN_GOLDEN=1.
